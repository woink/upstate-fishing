# 2026-02-11

## Infrastructure Decisions

### Forgejo VM is the Source of Truth
- All code and deployments should live on the Forgejo VM's data disk (not boot disk)
- Forgejo VM accessible via Tailscale at `100.78.158.46` (hostname: `forgejo`)
- SSH access via Tailscale SSH (enabled with `tailscale set --ssh`)

### SSH Access
- **Shell access:** `ssh forgejo-shell` (port 22, user wardprice, Tailscale SSH)
- **Git access:** `ssh forgejo` (port 2222, user git, for push/pull)
- Config added to `~/.ssh/config` on clawpei

### Tailscale ACLs
- Created `tag:server` for clawpei, forgejo, ci-runner
- Added grants for tag:server ↔ tag:server communication
- Added SSH ACLs for server-to-server and member-to-server access

### Repo Renamed
- `ward/upstate-fishing-backend` → `ward/upstate-fishing`
- Updated description: "Upstate NY/NJ fishing conditions app - Deno/TypeScript"

### Working Directory on Forgejo
- **Path:** `/mnt/data/projects/upstate-fishing`
- **Data disk:** `/mnt/data` (20GB, `/dev/sda`)
- **Boot disk:** `/dev/sdb` (20GB at `/`) — OS only
- Git remote: `http://localhost:3000/ward/upstate-fishing.git`

## TODO
- [x] Get SSH shell access to Forgejo VM
- [x] Set up working directory on Forgejo data disk
- [ ] Implement #3 Caching layer (in dedicated Telegram topic)
- [ ] Close issue #1 (Frontend complete)
